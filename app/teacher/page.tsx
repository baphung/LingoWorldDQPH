'use client'; import {useState} from 'react'; export default function Teacher(){const[text,setText]=useState('');const[msgs,setMsgs]=useState<string[]>(['Xin chào! Tôi là AI Teacher. Bạn muốn luyện từ vựng, ngữ pháp, nói hay viết?']);return <main className="section" style={{paddingTop:50,maxWidth:850}}><p className="eyebrow">AI TEACHER</p><h1 style={{fontFamily:'Georgia',fontSize:50}}>Giáo viên luôn sẵn sàng.</h1><div className="ai-card">{msgs.map((m,i)=><div className={'bubble '+(i%2?'user':'')} key={i}>{m}</div>)}<form onSubmit={e=>{e.preventDefault();if(!text.trim())return;setMsgs([...msgs,text,'Rất tốt! Tôi sẽ sửa câu và giải thích điểm cần cải thiện cho bạn.']);setText('')}} className="search"><input value={text} onChange={e=>setText(e.target.value)} placeholder="Viết câu bạn muốn luyện…"/><button>Gửi</button></form></div></main>}