import Link from 'next/link'; import {chineseLessons} from '../../../lib/data'; export default async function Lesson({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const l=chineseLessons.find(x=>x.id===slug)||chineseLessons[0];return <main className="section" style={{paddingTop:45,maxWidth:900}}><Link href="/learn/chinese">← HSK1</Link><p className="eyebrow" style={{marginTop:30}}>BÀI HỌC · {l.type.toUpperCase()}</p><h1 style={{fontFamily:'Georgia',fontSize:48}}>{l.title}</h1><p className="lead">{l.subtitle} · {l.minutes} phút · +{l.xp} XP</p><div className="grid" style={{marginTop:30}}>{l.words.map(w=><div className="lang" key={w[0]} style={{display:'block',textAlign:'center',padding:30}}><div style={{fontSize:42,fontWeight:800}}>{w[0]}</div><div style={{fontSize:18,marginTop:8}}>{w[1]}</div><small>{w[2]}</small></div>)}</div><div className="continue" style={{marginTop:30}}><div><b>🤖 AI Teacher</b><p>Hãy đọc to từng từ và đặt một câu đơn giản.</p></div><Link href="/teacher" className="primary">Hỏi AI Teacher</Link></div></main>}